# Set this to your Render backend URL when deploying to Vercel.
# Leave it unset for local development (the Vite proxy handles it automatically).
VITE_API_URL=https://your-render-app-name.onrender.com
